import { Card, CardContent } from '@/components/ui/card'

export const Mapping = () => {
  return (
    <Card>
      <CardContent className="text-muted-foreground">
        <p>Map columns to JSON fields.</p>
      </CardContent>
    </Card>
  )
}
